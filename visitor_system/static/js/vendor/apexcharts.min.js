!function(w){
  if(w.ApexCharts) return;
  function Stub(sel,opts){
    this.el = typeof sel==='string' ? document.querySelector(sel) : sel;
    this.opts = opts||{};
  }
  Stub.prototype.render = function(){
    if(!this.el) return Promise.resolve();
    var d=document.createElement('div');
    d.style.cssText='display:flex;align-items:center;justify-content:center;height:100%;color:#667382;background:rgba(98,105,118,.06);border:1px dashed #cbd3da;border-radius:6px;font:14px "Noto Sans",sans-serif;';
    d.textContent='ApexCharts fallback';
    this.el.innerHTML='';
    this.el.appendChild(d);
    return Promise.resolve();
  };
  Stub.prototype.updateOptions=function(){ return Promise.resolve(); };
  w.ApexCharts = Stub;
  try { console.warn('Local fallback ApexCharts stub loaded. Replace with real ApexCharts for full features.'); } catch(_){ }
}(window);


